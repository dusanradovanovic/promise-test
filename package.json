{
  "name": "promise-test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "watch_savefile": "tsnd --notify=false --respawn --ignore-watch node_modules --transpile-only -- ./src/savefile.ts",
    "test": "yarn jest",
    "watch_test": "yarn jest --watch"
  },
  "devDependencies": {
    "@types/jest": "^24.0.19",
    "jest": "^24.9.0",
    "ts-jest": "^24.1.0",
    "ts-node-dev": "^1.0.0-pre.43",
    "typescript": "^3.6.4"
  }
}
